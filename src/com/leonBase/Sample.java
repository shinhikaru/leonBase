package com.leonBase;
public class Sample {  public static void main(String arg[]) {  }    //URLクラスを用いてWebsiteの内容を読み込む。    public void botSimulator() {        try {            URL url = new URL("http://localhost:8080/stock");            URLConnection uc = url.openConnection();            String userAgent;                        // googleBotのユーザエージェントを設定する。            //userAgent = "Mozilla/5.0 (compatible; Googlebot/2.1; +http://www.google.com/bot.html)";            uc.setRequestProperty("User-Agent", userAgent);
            InputStream is = uc.getInputStream();                        BufferedReader reader = new BufferedReader(new InputStreamReader(is));            String s;            while ((s = reader.readLine()) != null) {                System.out.println(s);            }            reader.close();        } catch (Exception e) {            System.out.println(e.toString());        }    }}
